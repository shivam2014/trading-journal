/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    serverActions: true,
  },
  // Disable automatic static optimization for trade pages
  // as they depend on dynamic data
  unstable_runtimeJS: true,
  webpack: (config) => {
    // Handle WebSocket server integration
    config.externals.push({
      'utf-8-validate': 'commonjs utf-8-validate',
      'bufferutil': 'commonjs bufferutil',
    });
    return config;
  },
};

module.exports = nextConfig;